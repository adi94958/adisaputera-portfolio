// Local Storage Keys
export const STORAGE_KEYS = {
  THEME: 'theme-preference',
} as const;

// Environment
export const ENV = {
  DEV: 'development',
  PROD: 'production',
  TEST: 'test',
} as const;
